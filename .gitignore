__pycache__/
*.pyc
.DS_Store

# SQLite is a derived artifact rebuilt from data/ on every run, never committed.
*.db
*.sqlite3

# Rendered output: regenerated from data/ in seconds, so it is never committed.
site/
